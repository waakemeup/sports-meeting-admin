export interface OpeningInfo {
  name: string;
  theme: string;
  startDate: string;
  endDate: string;
  id: number;
  // key?: number; //TODO:也许应该把这个删了
  sport_id?: string;
  status?: boolean;
  name?: string;
  theme?: string;
  location?: string;
}
